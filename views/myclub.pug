include partials/head.pug

title Mijn club
    
body
    include partials/nav.pug
    main
      h1 Mijn club
      block content
        if data
          p Club: #{data.club}
          p Aantal jaar lid: #{data.time}
          p Wat vind ik leuk bij deze club: #{data.description}
          a.button(href='/profile') Naar profiel
          br
          br
          a.button#remove(href='/myclub', data-id=data._id) Verwijderen
          
    include partials/footer.pug
    script(src='../static/js/client.js')
